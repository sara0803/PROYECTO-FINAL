/*#include "escena.h"
#include "gifanimation.h"

Escena::Escena(QObject *parent)
    :QGraphicsScene(parent )  //dandole parametro a la clase base
{
    mGifAnimation=new GifAnimation(this);
    mGifAnimation->setFilename("");
}
*/
